_target_: src.data.gaze_datamodule.GazeDataModule
data_dir: ${paths.data_dir}
train_dataset: null
with_subject: false
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 0
pin_memory: false
aug: false